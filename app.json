{
  "name": "Banked's Topup Example",
  "description": "An [Express.js](https://expressjs.com/) top-up example demonstrating the Banked API",
  "repository": "https://github.com/banked/example-topup",
  "website": "https://banked-example-topup.herokuapp.com/",
  "env": {
    "BANKED_API_KEY": {
      "description": "Your Banked API key generated in the Banked console",
      "required": true
    },
    "BANKED_API_SECRET": {
      "description": "The Banked API secret generated in the Banked console",
      "required": true
    },
    "BANKED_CLIENT_KEY": {
      "description": "The Banked client key used to power the embedded checkout",
      "required": true
    },
    "BASE_URL": {
      "description": "The base url of the store, which is used to construct callback URL's Banked will direct to e.g. https://www.example.com/",
      "required": true
    },
    "PAYEE_NAME": {
      "description": "The name of the account holder where payments will be made e.g. 'Example Ltd.' or 'Lisa Frank",
      "required": true
    },
    "ACCOUNT_NUMBER": {
      "description": "The 8 digit account number for payments to be made into",
      "required": true
    },
    "SORT_CODE": {
      "description": "The 6 digit sort code for the bank account to be paid, with no hyhons",
      "required": true
    },
    "SIGNATURE_KEY": {
      "description": "The signature uploaded as part of Banked's webhook setup",
      "required": true
    },
    "CLEAR_TOKEN": {
      "description": "The token used for authenticating requests to clear storage",
      "required": true
    },
    "PROVIDER_ID": {
      "description": "The detault provider to used with batch payments, if nonw is specific in the UI",
      "required": true
    },
    "BANK_ACCOUNT_ID": {
      "description": "The account ID of the bank account connected in the Banked console",
      "required": true
    }
  }
}
